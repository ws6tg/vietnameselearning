spring:
  datasource:
    url: jdbc:mysql://localhost:3307/vietnameseapp?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=true
    username: root
    password: 525300
    driver-class-name: com.mysql.cj.jdbc.Driver
#    druid:
#      aop-patterns: com.atguigu.admin.*  #监控SpringBean
#      filters: stat,wall     # 底层开启功能，stat（sql监控），wall（防火墙）
#      stat-view-servlet: # 配置监控页功能
#        enabled: true
#        login-username: admin
#        login-password: admin
#        resetEnable: false
#      web-stat-filter: # 监控web
#        enabled: true
#        urlPattern: /*
#        exclusions: '*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*'
#      filter:
#        stat: # 对上面filters里面的stat的详细配置
#          slow-sql-millis: 1000
#          logSlowSql: true
#          enabled: true
#        wall:
#          enabled: true
#          config:
#            drop-table-allow: false
#      max-active: 32
  jackson:
    serialization:
  #格式化输出
     indent_output: true
  #忽略无法转换的对象
     fail_on_empty_beans: false
  #设置空如何序列化
    default-property-inclusion: non_null
  #日期格式化
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
#最大上传文件
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB

  #热部署
  devtools:
    restart:
      enabled: true
      additional-paths: 'src/main/java'
  #springboot-admin-client
  boot:
    admin:
      client:
        url: http://127.0.0.1:9090

#session过期时间
server:
  reactive:
    session:
      timeout: 0
  port: 8080
#日志管理
logging:
  level:
    root: INFO
  file:
    path: logs
    name: mylogs.log

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'

